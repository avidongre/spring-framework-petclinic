apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-framework-petclinic-helm.fullname" . }}
  labels:
    {{- include "spring-framework-petclinic-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-framework-petclinic-helm.selectorLabels" . | nindent 4 }}
